sample:
  description: example-standalone-inferencing-zephyr, the simplest Edge Impulse example for Zephyr
    application
  name: example-standalone-inferencing-zephyr
common:
    tags: introduction
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Hello World! (.*)"
tests:
  sample.basic.helloworld:
    tags: introduction
